# vDSO 问题分析与修改说明

## 一、问题背景

当前这套 vDSO 方案的目标，不是传统 Linux 那种“只给用户态加速若干系统调用入口”的 vDSO，而是：

- 内核态和用户态都可以执行 vDSO 中的代码；
- `vVAR` 中的数据希望能够共享；
- 最好代码页也能共享。

这意味着同一份 vDSO 镜像如果要同时被内核态和用户态使用，就必须满足一个额外条件：

- 这份镜像中的重定位结果，不能依赖某一个特定虚拟地址；
- 或者说，这份镜像被映射到不同虚拟地址后，页里的地址语义仍然必须正确。

而这次实际出现的问题是：

- 生成出来的 `libvdsoexample` 会在装载时按照“内核装载基址”做重定位；
- 之后原实现又把这份物理页映射到用户空间的另一个虚拟地址；
- 这样一来，用户执行时看到的代码地址虽然是用户基址，但镜像内部某些被修补过的地址仍然可能带着“内核基址”的语义；
- 最终表现为 `Segmentation fault (core dumped)`。

## 二、此前分析得到的关键结论

### 1. `map_linear()` 不是问题根源

结合 `axmm` 的实现可以确认：

- `map_linear()` 会直接建立有效的页表项；
- 它不是“只登记区域、不分配物理页”的懒映射；
- `populate_area()` 主要用于 `alloc/COW/file` 这类需要补页的 backend。

因此，这次 vDSO 崩溃不应优先怀疑为：

- “忘记调用 `populate_area()`，所以 vDSO 页不存在”。

### 2. 旧实现中的真正风险点

旧实现大致是这样的：

1. 启动时在内核地址空间中调用生成代码里的 `load_so()`；
2. 生成代码按内核虚拟地址完成 vDSO 的装载和重定位；
3. 手写代码记录这份镜像对应的物理地址；
4. 用户进程启动时，直接把这份物理页 `map_linear()` 到用户空间；
5. 同时把 `AT_SYSINFO_EHDR` 指向用户侧的 `vdso_start`。

这套做法的问题在于：

- 用户看到的入口地址确实是自己的 `vdso_start`；
- 但页里的重定位结果未必还是“相对用户基址正确”的；
- 于是用户一旦调用依赖这些重定位结果的路径，就会崩。

你实际运行时也已经打印出了这个关键现象：

- 用户 `vDSO` 基址和内核装载基址不同。

这与 fault 现象是吻合的。

## 三、这次修改的总体思路

为了先把用户态崩溃真正修掉，我做了一个折中但更稳妥的实现：

- 内核仍然保留一份自己的 vDSO 镜像，用于内核侧测试和调试；
- 用户进程启动时，不再直接映射“内核那份已经重定位好的 vDSO 代码页”；
- 而是在用户地址空间中动态找一段空闲区域，在用户最终的虚拟地址上重新装载一份用户专用的 vDSO 代码镜像；
- 然后再把 `vVAR` 那一段重新接回共享的物理页。

这样做的结果是：

- 用户代码页：按用户基址重定位，避免因为基址不一致而崩溃；
- `vVAR`：仍然共享；
- 代码页共享：暂时不再强行维持，因为当前生成器产物并不满足“同一份已重定位代码页可安全别名到两个虚拟地址”的条件。

## 四、实际修改了什么

### 1. 修改了 `vdso/src/lib.rs`

#### 1.1 保留并增强了内核侧布局信息

增加或保留了以下全局信息：

- `VDSO_KERNEL_BASE`
- `VDSO_START`
- `VDSO_SIZE`
- `VVAR_KERNEL_BASE`
- `VVAR_START`
- `VVAR_SIZE`

作用：

- 记录内核侧 vDSO/vVAR 的原始虚拟基址；
- 记录对应物理地址；
- 方便对比内核装载基址与用户运行基址。

#### 1.2 修正了 vDSO 大小统计方式

原先是简单把多个 region 的大小相加。  
现在改成了“真实跨度”计算：

- 起点取所有 region 的最小地址；
- 终点取所有 region 的最大结束地址；
- 大小 = `end - start`。

原因：

- 简单累加只在 region 连续且没有空洞时才正确；
- 真实跨度更稳妥，也更适合后续诊断。

#### 1.3 新增了用户态专用装载函数 `load_into_user`

这是本次修复最核心的改动。

它做了这些事：

1. 在调用生成代码的 `load_so()` 之前，先设置一个“用户装载上下文”；
2. 让生成代码通过 `MemIf::alloc()` 时，不再向内核申请一块普通内存，而是直接在目标用户地址空间里分配并建立映射；
3. 让生成代码通过 `MemIf::protect()` 改权限时，直接改的是当前用户地址空间里的映射权限；
4. 由于生成代码内部会直接按目标虚拟地址写内存，所以在真正调用 `load_so()` 前，临时把当前活跃页表切换到这个用户地址空间对应的页表；
5. 等生成代码把整段用户 vDSO/vVAR 装载并按用户基址重定位完成后，再把活跃页表切回去；
6. 最后把用户空间中的 `vVAR` 这一段取消掉，重新映射回共享的 `VVAR_START` 物理页。

这样就实现了：

- 用户代码页按用户基址装载和重定位；
- 用户 `vVAR` 继续共享内核那份物理页。

### 2. 修改了 `core/src/mm.rs`

#### 2.1 调整了用户 vDSO/vVAR 的放置方式

现在会在 `load_user_app()` 中：

- 先计算 `vdso_size` 和 `vvar_size`；
- 再在用户空间中动态寻找一段真正空闲的区域；
- 用这段区域来放置 `vVAR + vDSO`；
- 然后根据这个实际找到的位置计算：
  - `vvar_start`
  - `vdso_start`

这样做的原因是：

- 原先默认放在用户栈下方那一段，实际并不总是空闲；
- `find_free_area()` 找到的结果更符合当前地址空间真实状态；
- `AT_SYSINFO_EHDR` 也会对应到这次实际装载到的地址。

#### 2.2 不再直接 `map_linear()` 内核那份 vDSO 代码页

旧逻辑是：

- `map_linear()` 用户 vDSO 到 `VDSO_START`
- `map_linear()` 用户 vVAR 到 `VVAR_START`

现在改成：

- 调用 `vdso::load_into_user(uspace, vvar_start)`

也就是说：

- 用户 vDSO 代码改为“按用户基址现装”；
- 用户 vVAR 最终仍映射到共享物理页。

#### 2.3 调整了日志含义

现在的日志会明确说明：

- 用户基址和内核装载基址可能不同；
- 但用户路径会改用“用户专用代码镜像 + 共享 vVAR”的方式处理。

## 五、为什么这次修复是合理的

当前生成器产物的现实情况是：

- 它会在装载时使用某个虚拟基址做重定位；
- 这意味着代码页本身并不是天然“可在两个不同虚拟地址下同时正确执行”的。

如果仍然强行让：

- 内核和用户直接共享同一份已重定位代码页，

那么前提就变成：

- 生成代码必须完全满足“可别名执行”的条件。

但从当前现象看，这个条件并不成立。

所以本次选择的方案是一个更稳妥的中间态：

- 先恢复正确性；
- 保留 `vVAR` 共享；
- 暂时不强求代码页共享。

## 六、哪些文件没有直接修改

没有直接修改：

- `vdso_output/libvdsoexample/*`

原因：

- 这些内容是生成出来的；
- 直接改会在下次重新生成时失效。

## 七、如果后续想真正恢复“代码页也共享”，应该改哪里

如果你后续希望真正实现：

- 内核和用户共享同一份代码页；
- 并且这份代码页在两个虚拟地址下都能安全执行；

那么应该去改生成模板仓库，而不是改 `vdso_output/libvdsoexample`。

你提供的模板仓库是：

- `https://github.com/rosy233333/vdso_crate_template`

需要重点改的方向是：

### 1. 让生成出来的 vDSO 对“多虚拟地址别名执行”安全

也就是要保证：

- 重定位结果不把单一虚拟基址固化进镜像；
- 或者镜像内部所有关键地址都通过运行时相对计算得到；
- 或者装载器明确支持为不同地址空间生成不同代码镜像。

### 2. 明确生成器的地址契约

模板最好在文档中明确说明：

- 生成产物是否允许“同一份代码页映射到多个虚拟地址”；
- `vVAR` 和代码页的共享语义分别是什么；
- 哪些重定位会导致地址别名失效。

### 3. 暴露更多元数据

如果生成器能额外暴露这些信息，会很有帮助：

- 装载时使用的基址；
- 各个 region 的真实跨度；
- 是否包含依赖固定装载基址的重定位结果；
- 是否允许跨地址空间共享代码页。

## 八、后续验证建议

在重新运行 `make test` 后，建议重点看这几类现象：

1. 用户侧日志里是否还会出现“直接映射内核 vDSO 代码页”的痕迹；
2. 新日志是否显示用户 vDSO 是按用户基址装载的；
3. `vdso_test` 是否还能在第一次调用 `set_shared()` 时崩溃；
4. 如果仍崩溃，再对照：
   - `sepc`
   - `stval`
   - `scause`
   - 用户 vDSO 范围
   - 用户 vVAR 范围

如果此时 fault 地址仍然靠近内核基址，那说明还有别的路径在引用内核装载镜像；  
如果 fault 消失，则说明“内核已重定位代码页直接共享给用户”确实就是根因。
