[package]
name = "vdso_wrapper"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
vqueue = { path = "../../deps/vqueue_vdso", features = [] }
